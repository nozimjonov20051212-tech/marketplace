<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Investor Analytics | Pro Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Inter', sans-serif;
            background-color: #0f172a;
        }

        /* SAHIFA YUKLANISH PARDASI */
        .page-loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #0f172a; 
            z-index: 9999;
            transition: transform 0.8s cubic-bezier(0.77, 0, 0.175, 1);
            transform-origin: bottom;
        }
        .page-loader.hidden { transform: scaleY(0); }
        .loader-active { transform: scaleY(1) !important; transform-origin: top !important; }

        .glass-card {
            background: rgba(30, 41, 59, 0.5);
            backdrop-filter: blur(12px);
            border: 1px solid rgba(255, 255, 255, 0.05);
        }
    </style>
</head>
<body class="bg-[#0f172a] text-white min-h-screen p-4 md:p-10">
    <div id="loader" class="page-loader"></div>
    
    <div class="max-w-7xl mx-auto">
        <div class="flex flex-col md:flex-row justify-between items-center mb-12 gap-6" data-aos="fade-down">
            <div class="flex items-center gap-4 flex-1">
                <div class="h-16 w-16 flex items-center justify-center bg-white rounded-2xl p-2 shadow-2xl border border-white/20 transform hover:rotate-12 transition">
                    <img src="/irrigatsiya_logo.PNG" alt="TIQXMMI Logo" class="h-full w-full object-contain">
                </div>
                <div class="hidden sm:block">
                    <h2 class="text-white font-black text-xl leading-none tracking-tighter">TIQXMMI</h2>
                    <p class="text-[10px] text-blue-400 font-bold uppercase tracking-[0.2em] mt-1">Innovation Hub</p>
                </div>
            </div>

            <div class="text-center flex-[2]">
                <h1 class="text-4xl font-extrabold bg-gradient-to-r from-blue-400 to-emerald-400 bg-clip-text text-transparent uppercase tracking-tight">
                    Investor Analytics
                </h1>
                <p class="text-slate-400 mt-1 text-sm uppercase tracking-widest opacity-70 italic">Strategik tahlillar va investitsiya oqimi</p>
            </div>

            <div class="flex justify-end flex-1 w-full md:w-auto">
                <a href="/" class="group flex items-center gap-2 bg-slate-800 hover:bg-blue-600 px-6 py-3 rounded-2xl transition-all duration-300 border border-slate-700 shadow-lg">
                    <i class="fas fa-home group-hover:-translate-y-1 transition text-blue-400 group-hover:text-white"></i>
                    <span class="font-bold text-sm uppercase tracking-wider group-hover:text-white">Bosh sahifa</span>
                </a>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-3 gap-8 mb-10">
            <div class="lg:col-span-2 glass-card p-8 rounded-[2.5rem] shadow-2xl" data-aos="fade-right" data-aos-delay="200">
                <div class="flex justify-between items-center mb-6">
                    <h3 class="text-xl font-bold text-slate-200">Startup Funding Growth</h3>
                    <span class="text-emerald-400 text-sm bg-emerald-400/10 px-4 py-1.5 rounded-full border border-emerald-400/20 font-bold animate-pulse">+24% o'sish</span>
                </div>
                <canvas id="growthChart" height="140"></canvas>
            </div>

            <div class="bg-gradient-to-br from-blue-600/20 to-purple-600/20 backdrop-blur-md p-8 rounded-[2.5rem] border border-blue-500/20 shadow-2xl flex flex-col items-center justify-center text-center h-full min-h-[350px] relative overflow-hidden" data-aos="fade-left" data-aos-delay="400">
                <div class="relative mb-6 flex items-center justify-center">
                    <div class="absolute w-32 h-32 bg-blue-500/20 rounded-full animate-ping"></div>
                    <div class="relative bg-gradient-to-tr from-blue-600 to-emerald-500 p-7 rounded-full shadow-2xl shadow-blue-500/50">
                        <i class="fas fa-chart-line text-4xl text-white"></i>
                    </div>
                </div>
                <h4 class="text-xl font-bold mt-2 text-white">Jonli tahlil</h4>
                <p class="text-slate-400 mt-3 text-sm leading-relaxed max-w-[220px]">Real-vaqt rejimida investitsiya oqimi nazorat qilinmoqda</p>
                <div class="mt-6 flex items-end gap-1.5 h-8">
                    <div class="w-1.5 h-4 bg-blue-500 rounded-full animate-bounce" style="animation-delay: 0.1s"></div>
                    <div class="w-1.5 h-7 bg-blue-400 rounded-full animate-bounce" style="animation-delay: 0.2s"></div>
                    <div class="w-1.5 h-5 bg-emerald-400 rounded-full animate-bounce" style="animation-delay: 0.3s"></div>
                    <div class="w-1.5 h-8 bg-blue-300 rounded-full animate-bounce" style="animation-delay: 0.4s"></div>
                </div>
            </div>
        </div>

        <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
            <div class="glass-card p-8 rounded-[2.5rem] shadow-2xl" data-aos="zoom-in" data-aos-delay="500">
                <h3 class="text-xl font-bold mb-6 text-slate-200 text-center uppercase tracking-wider">Yo'nalishlar taqsimoti</h3>
                <div class="max-w-[280px] mx-auto">
                    <canvas id="categoryChart"></canvas>
                </div>
            </div>

            <div class="glass-card p-8 rounded-[2.5rem] shadow-2xl relative overflow-hidden" data-aos="zoom-in" data-aos-delay="600">
                <div class="absolute top-0 right-0 p-6 opacity-5">
                    <i class="fas fa-rocket text-blue-500 text-9xl"></i>
                </div>
                <h2 class="text-2xl font-bold mb-6 text-blue-400 flex items-center gap-3 uppercase tracking-tight">
                    <i class="fas fa-hand-holding-usd"></i> Investitsiya kiritish
                </h2>
                <form id="investForm" class="space-y-5">
                    <div>
                        <label class="block text-xs font-bold text-slate-500 uppercase tracking-widest mb-2 ml-1">To'liq ismingiz</label>
                        <input type="text" id="investorName" placeholder="Jasur Nozimjonov" required class="w-full bg-slate-900/80 border border-slate-700 p-4 rounded-2xl outline-none focus:border-blue-500 transition-all text-white placeholder:text-slate-600">
                    </div>
                    <div>
                        <label class="block text-xs font-bold text-slate-500 uppercase tracking-widest mb-2 ml-1">Investitsiya miqdori ($)</label>
                        <input type="number" id="investAmount" placeholder="5000" required class="w-full bg-slate-900/80 border border-slate-700 p-4 rounded-2xl outline-none focus:border-blue-500 transition-all text-white placeholder:text-slate-600">
                    </div>
                    <button type="submit" id="submitBtn" class="relative overflow-hidden w-full bg-gradient-to-r from-blue-600 to-blue-400 hover:from-blue-500 hover:to-blue-300 py-4 rounded-2xl font-black text-lg shadow-lg shadow-blue-500/20 transition-all active:scale-[0.98] group uppercase tracking-widest">
                        <span class="flex items-center justify-center gap-2">
                            <i class="fas fa-paper-plane group-hover:translate-x-1 group-hover:-translate-y-1 transition-transform"></i>
                            <span id="btnText">So'rov yuborish</span>
                        </span>
                    </button>
                </form>
            </div>
        </div>
    </div>

    <div id="successModal" class="fixed inset-0 z-[10000] flex items-center justify-center hidden bg-slate-950/90 backdrop-blur-md p-4">
        <div class="bg-slate-800 border border-slate-700 p-8 rounded-[3rem] shadow-2xl max-w-sm w-full text-center transform transition-all scale-90 opacity-0" id="modalContent">
            <div class="w-24 h-24 bg-emerald-500/20 rounded-full flex items-center justify-center mx-auto mb-6 relative">
                <div class="absolute inset-0 rounded-full bg-emerald-500/30 animate-ping"></div>
                <i class="fas fa-check text-5xl text-emerald-500"></i>
            </div>
            <h3 class="text-3xl font-black text-white mb-2">Rahmat!</h3>
            <p class="text-slate-400 mb-8 font-medium" id="modalMessage">Sizning so'rovingiz qabul qilindi.</p>
            <button onclick="closeModal()" class="w-full bg-emerald-600 hover:bg-emerald-500 text-white font-black py-4 rounded-2xl transition-all shadow-xl shadow-emerald-500/20 uppercase tracking-widest">
                Tushunarli
            </button>
        </div>
    </div>

    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ once: true, duration: 800 });

        // SAHIFA O'TISH ANIMATSIYASI
        window.addEventListener('load', () => {
            const loader = document.getElementById('loader');
            setTimeout(() => { loader.classList.add('hidden'); }, 200);
        });

        document.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', function(e) {
                if (this.hostname === window.location.hostname && !this.hash && !this.target) {
                    e.preventDefault();
                    const destination = this.href;
                    const loader = document.getElementById('loader');
                    loader.classList.remove('hidden');
                    loader.classList.add('loader-active');
                    setTimeout(() => { window.location.href = destination; }, 600);
                }
            });
        });

        // CHARTS SETUP
        Chart.defaults.color = '#94a3b8';
        Chart.defaults.font.family = 'Inter, sans-serif';

        new Chart(document.getElementById('growthChart'), {
            type: 'line',
            data: {
                labels: ['Yan', 'Fev', 'Mar', 'Apr', 'May', 'Iyun'],
                datasets: [{
                    label: 'Investitsiya',
                    data: [12000, 19000, 15000, 25000, 22000, 30000],
                    borderColor: '#3b82f6',
                    borderWidth: 4,
                    pointBackgroundColor: '#3b82f6',
                    pointBorderColor: '#fff',
                    pointHoverRadius: 8,
                    tension: 0.4,
                    fill: true,
                    backgroundColor: (context) => {
                        const ctx = context.chart.ctx;
                        const gradient = ctx.createLinearGradient(0, 0, 0, 400);
                        gradient.addColorStop(0, 'rgba(59, 130, 246, 0.3)');
                        gradient.addColorStop(1, 'rgba(59, 130, 246, 0)');
                        return gradient;
                    }
                }]
            },
            options: { responsive: true, plugins: { legend: { display: false } } }
        });

        new Chart(document.getElementById('categoryChart'), {
            type: 'doughnut',
            data: {
                labels: ['IT', 'Agro', 'Med', 'Edu'],
                datasets: [{
                    data: [40, 25, 20, 15],
                    backgroundColor: ['#3b82f6', '#10b981', '#8b5cf6', '#f59e0b'],
                    borderWidth: 0,
                    hoverOffset: 25
                }]
            },
            options: { cutout: '80%', plugins: { legend: { position: 'bottom', labels: { padding: 30, usePointStyle: true, font: { weight: 'bold' } } } } }
        });

        // FORM HANDLING
        const modal = document.getElementById('successModal');
        const modalContent = document.getElementById('modalContent');

        document.getElementById('investForm').addEventListener('submit', function(e) {
            e.preventDefault();
            const btn = document.getElementById('submitBtn');
            const btnText = document.getElementById('btnText');
            const name = document.getElementById('investorName').value;

            btn.disabled = true;
            btnText.innerHTML = "Yuborilmoqda...";

            setTimeout(() => {
                document.getElementById('modalMessage').innerText = `Rahmat ${name}! Sizning so'rovingiz muvaffaqiyatli yuborildi.`;
                modal.classList.remove('hidden');
                setTimeout(() => {
                    modalContent.classList.remove('scale-90', 'opacity-0');
                    modalContent.classList.add('scale-100', 'opacity-100');
                }, 50);

                btn.disabled = false;
                btnText.innerHTML = "So'rov yuborish";
                document.getElementById('investForm').reset();
            }, 1500);
        });

        function closeModal() {
            modalContent.classList.remove('scale-100', 'opacity-100');
            modalContent.classList.add('scale-90', 'opacity-0');
            setTimeout(() => { modal.classList.add('hidden'); }, 300);
        }
    </script>
</body>
</html>