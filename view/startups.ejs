<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Startaplar | TIQXMMI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Inter', sans-serif;
            background-color: #0f172a;
        }

        .page-loader {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: #0f172a; z-index: 9999;
            transition: transform 0.8s cubic-bezier(0.77, 0, 0.175, 1);
            transform-origin: bottom;
        }
        .page-loader.hidden { transform: scaleY(0); }

        /* FON TASVIRI VA GRADIENT OLIB TASHLANDI */
        .main-bg {
            background-color: #0f172a;
            min-height: 100vh;
        }

        .startup-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 2.5rem;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .startup-card:hover {
            border-color: rgba(59, 130, 246, 0.5);
            background: rgba(255, 255, 255, 0.07);
            transform: translateY(-10px);
            box-shadow: 0 20px 40px -15px rgba(59, 130, 246, 0.3);
        }

        .glass-input {
            background: rgba(15, 23, 42, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
        }
        .glass-input:focus { border-color: #3b82f6; box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1); }
    </style>
</head>
<body class="main-bg text-white">
    <div id="loader" class="page-loader"></div>

    <div class="max-w-7xl mx-auto px-6 py-6 flex justify-between items-center sticky top-0 z-50 backdrop-blur-md bg-[#0f172a]/40 rounded-b-3xl">
        <a href="/" class="group flex items-center gap-2 bg-slate-900/80 hover:bg-blue-600 px-5 py-2.5 rounded-2xl transition-all border border-slate-700">
            <i class="fas fa-arrow-left text-blue-400 group-hover:text-white transition-transform group-hover:-translate-x-1"></i>
            <span class="font-bold text-xs uppercase tracking-widest group-hover:text-white">Asosiy</span>
        </a>
        
        <% if (locals.isLoggedIn) { %>
            <div class="flex items-center gap-2 bg-emerald-500/10 border border-emerald-500/20 px-4 py-2.5 rounded-2xl">
                <span class="text-emerald-400 font-black uppercase text-[10px] tracking-tighter">Admin Rejim</span>
                <a href="/logout" class="text-red-400 hover:text-red-300 text-[10px] font-black uppercase border-l border-white/10 pl-3">Chiqish</a>
            </div>
        <% } %>
    </div>

    <header class="text-center mt-10 mb-16" data-aos="zoom-in">
        <h1 class="text-6xl md:text-7xl font-black uppercase tracking-tighter">
            Star<span class="text-blue-500">tup</span>lar
        </h1>
        <div class="w-20 h-1.5 bg-blue-600 mx-auto mt-4 rounded-full"></div>
        <p class="text-slate-400 text-sm mt-4 font-bold uppercase tracking-[0.3em] opacity-80">Universitetimizning istiqbolli loyihalari</p>
    </header>

    <% if (locals.isLoggedIn) { %>
    <div class="max-w-4xl mx-auto px-6 mb-16" data-aos="fade-up">
        <div class="bg-slate-800/40 backdrop-blur-2xl p-10 rounded-[3rem] border border-blue-500/30 shadow-2xl">
            <h2 class="text-2xl font-bold mb-8 flex items-center gap-3 text-blue-400 uppercase tracking-tight">
                <i class="fas fa-rocket"></i> Yangi loyiha qo'shish
            </h2>
            <form action="/admin/add-item" method="POST" enctype="multipart/form-data" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <input type="hidden" name="type" value="startup">
                <div class="space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Loyiha nomi</label>
                    <input type="text" name="title" placeholder="Masalan: Agrotech AI" required class="glass-input w-full p-4 rounded-2xl outline-none text-white">
                </div>
                <div class="space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Kerakli investitsiya ($)</label>
                    <input type="text" name="goal" placeholder="50,000" required class="glass-input w-full p-4 rounded-2xl outline-none text-white">
                </div>
                <div class="space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Loyiha holati</label>
                    <input type="text" name="status" placeholder="MVP / G'oya / Tayyor" class="glass-input w-full p-4 rounded-2xl outline-none text-white">
                </div>
                <div class="space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Loyiha rasmi</label>
                    <div class="bg-slate-900/40 p-2.5 rounded-2xl border border-slate-700">
                        <input type="file" name="image" accept="image/*" class="w-full text-xs text-slate-400 file:mr-4 file:py-2 file:px-4 file:rounded-xl file:border-0 file:text-xs file:font-black file:bg-blue-600 file:text-white cursor-pointer">
                    </div>
                </div>
                <div class="md:col-span-2 space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Loyiha haqida batafsil</label>
                    <textarea name="description" placeholder="Loyiha maqsadi va afzalliklari..." required class="glass-input w-full p-4 rounded-2xl outline-none text-white h-28"></textarea>
                </div>
                <button type="submit" class="md:col-span-2 bg-gradient-to-r from-blue-700 to-blue-500 py-5 rounded-2xl font-black text-white shadow-xl shadow-blue-500/20 uppercase tracking-widest active:scale-95 transition-all">
                    Loyihani joylash
                </button>
            </form>
        </div>
    </div>
    <% } %>

    <main class="max-w-7xl mx-auto px-6 pb-32">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
            <% if (locals.startups && startups.length > 0) { %>
                <% startups.forEach((startup, index) => { %>
                    <div class="startup-card p-8 group flex flex-col relative" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
                        <% if (locals.isLoggedIn) { %>
                            <form action="/admin/delete/startup/<%= startup.id %>" method="POST" onsubmit="return confirm('O\'chirilsinmi?')" class="absolute top-6 right-6 z-10">
                                <button type="submit" class="bg-red-500/20 hover:bg-red-500 text-red-500 hover:text-white w-10 h-10 rounded-2xl transition-all flex items-center justify-center border border-red-500/30 backdrop-blur-md">
                                    <i class="fas fa-trash-alt"></i>
                                </button>
                            </form>
                        <% } %>

                        <div class="relative overflow-hidden rounded-[2rem] mb-8 shadow-2xl aspect-video border border-white/5">
                            <img src="<%= startup.image %>" class="w-full h-full object-cover group-hover:scale-110 transition duration-700" onerror="this.src='/images/bino.jpeg'">
                            <div class="absolute inset-0 bg-gradient-to-t from-[#0f172a]/80 to-transparent"></div>
                            <span class="absolute bottom-4 left-4 bg-emerald-600/80 backdrop-blur-md text-[9px] font-black px-4 py-1.5 rounded-full uppercase tracking-widest">
                                <%= startup.status || 'Loyiha' %>
                            </span>
                        </div>
                        
                        <h3 class="text-2xl font-bold mb-3 group-hover:text-blue-400 transition-colors"><%= startup.title %></h3>
                        <p class="text-slate-400 text-sm leading-relaxed mb-8 flex-grow line-clamp-4"><%= startup.description %></p>
                        
                        <div class="flex items-center justify-between pt-6 border-t border-white/10">
                            <div>
                                <span class="text-[9px] text-slate-500 uppercase font-black block tracking-tighter mb-1 text-emerald-500">Investitsiya</span>
                                <span class="text-2xl font-black text-white">
                                    $<%= startup.goal %>
                                </span>
                            </div>
                            <a href="/contact" class="bg-blue-600 hover:bg-white hover:text-blue-600 text-white px-6 py-3 rounded-2xl transition-all font-black text-[10px] uppercase tracking-widest active:scale-90 shadow-lg shadow-blue-600/20">
                                Bog'lanish
                            </a>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-span-full text-center py-32 bg-white/5 rounded-[4rem] border border-dashed border-white/10">
                    <i class="fas fa-rocket text-5xl text-slate-700 mb-6"></i>
                    <p class="text-slate-500 font-bold uppercase tracking-widest italic">Hozircha startaplar yo'q.</p>
                </div>
            <% } %>
        </div>
    </main>

    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ once: true, duration: 1000 });
        window.addEventListener('load', () => {
            document.getElementById('loader').classList.add('hidden');
        });
    </script>
</body>
</html>