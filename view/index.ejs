<<<<<<< HEAD
<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>University Innovation Marketplace | TIQXMMI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Inter', sans-serif;
            background-color: #0f172a;
        }

        /* SAHIFA YUKLANISH PARDASI */
        .page-loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #0f172a; 
            z-index: 9999;
            transition: transform 0.8s cubic-bezier(0.77, 0, 0.175, 1);
            transform-origin: bottom;
        }
        .page-loader.hidden { transform: scaleY(0); }
        .loader-active { transform: scaleY(1) !important; transform-origin: top !important; }

        /* ASOSIY FON */
        .hero {
            background: linear-gradient(rgba(15, 23, 42, 0.6), rgba(15, 23, 42, 0.7)), 
                        url('/images/Rektorat.jpg');
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            height: 100vh;
        }

        /* SHISHA EFFEKTI */
        .glass-nav {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .nav-link {
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .nav-link:hover {
            background: rgba(59, 130, 246, 0.2);
            transform: translateY(-5px);
            color: #60a5fa; 
            box-shadow: 0 10px 20px -10px rgba(59, 130, 246, 0.5);
        }

        .login-btn {
            background: linear-gradient(135deg, #2563eb, #0891b2);
            transition: all 0.3s ease;
        }

        .login-btn:hover {
            box-shadow: 0 10px 25px -5px rgba(37, 99, 235, 0.4);
            transform: scale(1.05);
        }

        .animate-gradient {
            background-size: 200% 200%;
            animation: gradientMove 4s ease infinite;
        }

        @keyframes gradientMove {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
    </style>
</head>
<body class="hero flex flex-col justify-between overflow-hidden">
    <div id="loader" class="page-loader"></div>
    
    <header class="w-full max-w-7xl mx-auto px-6 py-8 flex items-center justify-between" data-aos="fade-down" data-aos-duration="1000">
        <div class="flex items-center gap-4">
            <div class="h-20 w-20 flex items-center justify-center transform hover:scale-110 transition duration-500">
                <img src="/erasebg-transformed.png" alt="TIQXMMI Logo" class="h-full w-full object-contain filter drop-shadow-[0_0_15px_rgba(255,255,255,0.2)]">
            </div>
            <div class="text-white">
                <h2 class="font-black text-2xl leading-none tracking-tighter">TIQXMMI</h2>
                <p class="text-[10px] font-bold uppercase tracking-[0.4em] text-blue-400 mt-1">Innovation Hub</p>
            </div>
        </div>

        <div class="flex items-center gap-4">
            <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
                <div class="flex items-center gap-2">
                    <span class="text-emerald-400 text-[10px] font-bold uppercase tracking-widest mr-2">Admin Active</span>
                    <a href="/logout" class="bg-red-500/20 text-red-400 hover:bg-red-500 hover:text-white px-4 py-3 rounded-2xl text-xs font-bold transition border border-red-500/30">
                        <i class="fas fa-sign-out-alt"></i>
                    </a>
                </div>
            <% } else { %>
                <a href="/login" class="login-btn text-white px-8 py-3 rounded-2xl font-black shadow-2xl uppercase tracking-widest text-xs border border-white/10">
                    <i class="fas fa-user-circle mr-2 text-lg"></i> Tizimga kirish
                </a>
            <% } %>
        </div>
    </header>

    <main class="flex-grow flex flex-col items-center justify-center px-4 text-center">
        <div data-aos="zoom-out-up" data-aos-duration="1200">
            <h1 class="text-white text-5xl md:text-7xl lg:text-8xl font-black leading-[0.9] tracking-tighter uppercase mb-6">
                TIQXMMI <br>
                <span class="animate-gradient text-transparent bg-clip-text bg-gradient-to-r from-blue-400 via-emerald-400 to-blue-500">
                    MARKETPLACE
                </span>
            </h1>
            <p class="text-slate-200 text-lg md:text-xl max-w-2xl mx-auto font-medium leading-relaxed mb-12 drop-shadow-lg opacity-90">
                Universitetimizning eng so'nggi innovatsiyalari, startuplari va intellektual mulklari jamlangan yagona ekotizim.
            </p>
        </div>

        <nav class="glass-nav p-2 rounded-[2.5rem] flex flex-wrap justify-center gap-2 max-w-fit mx-auto shadow-2xl" 
             data-aos="fade-up" data-aos-delay="400" data-aos-duration="1000">
            <a href="/products" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-shopping-bag mr-2 opacity-70"></i> Mahsulotlar
            </a>
            <a href="/startups" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-rocket mr-2 opacity-70"></i> Startaplar
            </a>
            <a href="/patents" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-certificate mr-2 opacity-70"></i> Patentlar
            </a>
            <a href="/investors" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-hand-holding-usd mr-2 opacity-70"></i> Investorlar
            </a>
            <a href="/contact" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-headset mr-2 opacity-70"></i> Bog'lanish
            </a>
            <a href="/education" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-user-graduate mr-2 opacity-70"></i> Ta'lim xizmatlari
            </a>
        </nav>
    </main>

    <footer class="w-full flex flex-col md:flex-row justify-between items-center px-12 py-6 bg-black/40 backdrop-blur-md border-t border-white/5" 
            data-aos="fade-up" data-aos-offset="0">
        <div class="text-white/40 text-[10px] uppercase font-black tracking-[0.3em] mb-4 md:mb-0">
            © 2026 TIQXMMI Innovatsiyalar Markazi • Barcha huquqlar himoyalangan
        </div>
        
        <div class="flex items-center gap-8">
            <div class="text-white/60 text-xs font-bold uppercase tracking-widest">
                Created by: <span class="text-blue-400 font-black border-b-2 border-blue-500/50 pb-1">Jasurbek Nozimjonov</span>
            </div>
            <div class="flex gap-4 text-white/50 text-xl">
                <a href="#" class="hover:text-blue-400 transition transform hover:scale-125"><i class="fab fa-telegram"></i></a>
                <a href="#" class="hover:text-pink-500 transition transform hover:scale-125"><i class="fab fa-instagram"></i></a>
            </div>
        </div>
    </footer>

    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ once: true, easing: 'ease-out-cubic' });

        window.addEventListener('load', () => {
            const loader = document.getElementById('loader');
            setTimeout(() => { loader.classList.add('hidden'); }, 200);
        });

        document.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', function(e) {
                if (this.hostname === window.location.hostname && !this.hash && !this.target) {
                    e.preventDefault();
                    const destination = this.href;
                    const loader = document.getElementById('loader');
                    loader.classList.remove('hidden');
                    loader.classList.add('loader-active');
                    setTimeout(() => { window.location.href = destination; }, 600);
                }
            });
        });
    </script>
</body>
=======
<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>University Innovation Marketplace | TIQXMMI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Inter', sans-serif;
            background-color: #0f172a;
        }

        /* SAHIFA YUKLANISH PARDASI (YANGI) */
        .page-loader {
            position: fixed;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: #0f172a; 
            z-index: 9999;
            transition: transform 0.8s cubic-bezier(0.77, 0, 0.175, 1);
            transform-origin: bottom;
        }
        .page-loader.hidden { transform: scaleY(0); }
        .loader-active { transform: scaleY(1) !important; transform-origin: top !important; }

        /* ASOSIY FON */
        .hero {
            background: linear-gradient(rgba(15, 23, 42, 0.6), rgba(15, 23, 42, 0.7)), 
                        url('/images/bino.jpeg'); 
            background-size: cover;
            background-position: center;
            background-attachment: fixed;
            height: 100vh;
        }

        /* SHISHA EFFEKTI */
        .glass-nav {
            background: rgba(255, 255, 255, 0.05);
            backdrop-filter: blur(15px);
            -webkit-backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.1);
        }

        .nav-link {
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .nav-link:hover {
            background: rgba(59, 130, 246, 0.2);
            transform: translateY(-5px);
            color: #60a5fa; 
            box-shadow: 0 10px 20px -10px rgba(59, 130, 246, 0.5);
        }

        .login-btn {
            background: linear-gradient(135deg, #2563eb, #0891b2);
            transition: all 0.3s ease;
        }

        .login-btn:hover {
            box-shadow: 0 10px 25px -5px rgba(37, 99, 235, 0.4);
            transform: scale(1.05);
        }

        .animate-gradient {
            background-size: 200% 200%;
            animation: gradientMove 4s ease infinite;
        }

        @keyframes gradientMove {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }
    </style>
</head>
<body class="hero flex flex-col justify-between overflow-hidden">
    <div id="loader" class="page-loader"></div>
    
    <header class="w-full max-w-7xl mx-auto px-6 py-8 flex items-center justify-between" data-aos="fade-down" data-aos-duration="1000">
        <div class="flex items-center gap-4">
            <div class="h-16 w-16 bg-white rounded-2xl p-2 shadow-2xl transform hover:rotate-12 transition">
                <img src="/irrigatsiya_logo.PNG" alt="TIQXMMI Logo" class="h-full w-full object-contain">
            </div>
            <div class="text-white">
                <h2 class="font-black text-2xl leading-none tracking-tighter">TIQXMMI</h2>
                <p class="text-[10px] font-bold uppercase tracking-[0.4em] text-blue-400 mt-1">Innovation Hub</p>
            </div>
        </div>

        <div class="flex items-center gap-4">
            <% if (typeof isLoggedIn !== 'undefined' && isLoggedIn) { %>
                <div class="flex items-center gap-2">
                    <span class="text-emerald-400 text-[10px] font-bold uppercase tracking-widest mr-2">Admin Active</span>
                    <a href="/logout" class="bg-red-500/20 text-red-400 hover:bg-red-500 hover:text-white px-4 py-3 rounded-2xl text-xs font-bold transition border border-red-500/30">
                        <i class="fas fa-sign-out-alt"></i>
                    </a>
                </div>
            <% } else { %>
                <a href="/login" class="login-btn text-white px-8 py-3 rounded-2xl font-black shadow-2xl uppercase tracking-widest text-xs border border-white/10">
                    <i class="fas fa-user-circle mr-2 text-lg"></i> Tizimga kirish
                </a>
            <% } %>
        </div>
    </header>

    <main class="flex-grow flex flex-col items-center justify-center px-4 text-center">
        <div data-aos="zoom-out-up" data-aos-duration="1200">
            <h1 class="text-white text-5xl md:text-7xl lg:text-8xl font-black leading-[0.9] tracking-tighter uppercase mb-6">
                TIQXMMI <br>
                <span class="animate-gradient text-transparent bg-clip-text bg-gradient-to-r from-blue-400 via-emerald-400 to-blue-500">
                    MARKETPLACE
                </span>
            </h1>
            <p class="text-slate-200 text-lg md:text-xl max-w-2xl mx-auto font-medium leading-relaxed mb-12 drop-shadow-lg opacity-90">
                Universitetimizning eng so'nggi innovatsiyalari, startuplari va intellektual mulklari jamlangan yagona ekotizim.
            </p>
        </div>

        <nav class="glass-nav p-2 rounded-[2.5rem] flex flex-wrap justify-center gap-2 max-w-fit mx-auto shadow-2xl" 
             data-aos="fade-up" data-aos-delay="400" data-aos-duration="1000">
            <a href="/products" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-shopping-bag mr-2 opacity-70"></i> Mahsulotlar
            </a>
            <a href="/startups" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-rocket mr-2 opacity-70"></i> Startuplar
            </a>
            <a href="/patents" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-certificate mr-2 opacity-70"></i> Patentlar
            </a>
            <a href="/investors" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-hand-holding-usd mr-2 opacity-70"></i> Investorlar
            </a>
            <a href="/contact" class="nav-link px-8 py-4 rounded-full text-white text-sm font-bold uppercase tracking-wider">
                <i class="fas fa-headset mr-2 opacity-70"></i> Bog'lanish
            </a>
        </nav>
    </main>

    <footer class="w-full flex flex-col md:flex-row justify-between items-center px-12 py-6 bg-black/40 backdrop-blur-md border-t border-white/5" 
            data-aos="fade-up" data-aos-offset="0">
        <div class="text-white/40 text-[10px] uppercase font-black tracking-[0.3em] mb-4 md:mb-0">
            © 2026 TIQXMMI Innovatsiyalar Markazi • Barcha huquqlar himoyalangan
        </div>
        
        <div class="flex items-center gap-8">
            <div class="text-white/60 text-xs font-bold uppercase tracking-widest">
                Created by: <span class="text-blue-400 font-black border-b-2 border-blue-500/50 pb-1">Jasurbek Nozimjonov</span>
            </div>
            <div class="flex gap-4 text-white/50 text-xl">
                <a href="#" class="hover:text-blue-400 transition transform hover:scale-125"><i class="fab fa-telegram"></i></a>
                <a href="#" class="hover:text-pink-500 transition transform hover:scale-125"><i class="fab fa-instagram"></i></a>
            </div>
        </div>
    </footer>

    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        // AOSni ishga tushirish
        AOS.init({
            once: true,
            easing: 'ease-out-cubic'
        });

        // SAHIFA O'TISH ANIMATSIYASI (YANGI JS)
        window.addEventListener('load', () => {
            const loader = document.getElementById('loader');
            setTimeout(() => { loader.classList.add('hidden'); }, 200);
        });

        document.querySelectorAll('a').forEach(link => {
            link.addEventListener('click', function(e) {
                if (this.hostname === window.location.hostname && !this.hash && !this.target) {
                    e.preventDefault();
                    const destination = this.href;
                    const loader = document.getElementById('loader');
                    loader.classList.remove('hidden');
                    loader.classList.add('loader-active');
                    setTimeout(() => { window.location.href = destination; }, 600);
                }
            });
        });
    </script>
</body>
>>>>>>> bec3915a8d75b2b7855842e7b1cd3461e2bc096d
</html>