<<<<<<< HEAD
<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Marketplace | TIQXMMI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Inter', sans-serif;
            background-color: #0f172a;
        }

        /* FON OLIB TASHLANDI */
        .fixed-bg-layer {
            display: none;
        }

        .page-loader {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: #0f172a; z-index: 9999;
            transition: transform 0.8s cubic-bezier(0.77, 0, 0.175, 1);
            transform-origin: bottom;
        }
        .page-loader.hidden { transform: scaleY(0); }
        .loader-active { transform: scaleY(1) !important; transform-origin: top !important; }

        .main-bg {
            min-height: 100vh;
            width: 100%;
        }

        .product-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 2.5rem;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
            height: fit-content;
            align-self: start;
        }

        .product-card:hover {
            border-color: rgba(59, 130, 246, 0.5);
            background: rgba(255, 255, 255, 0.07);
            transform: translateY(-10px);
            box-shadow: 0 20px 40px -15px rgba(59, 130, 246, 0.3);
        }

        .description-text {
            display: -webkit-box;
            -webkit-line-clamp: 3; 
            -webkit-box-orient: vertical;  
            overflow: hidden;
            transition: all 0.3s ease;
        }

        .description-text.expanded {
            -webkit-line-clamp: unset; 
        }

        .btn-more {
            color: #3b82f6;
            font-size: 0.75rem;
            font-weight: 800;
            text-transform: uppercase;
            letter-spacing: 0.05em;
            margin-bottom: 1.5rem;
            display: inline-block;
            cursor: pointer;
        }

        @keyframes cartPop {
            0% { transform: scale(1); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
        .animate-pop { animation: cartPop 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275); }

        #cart-modal {
            display: none; position: fixed; inset: 0; z-index: 10000;
            background: rgba(15, 23, 42, 0.9); backdrop-filter: blur(20px);
            align-items: center; justify-content: center;
        }

        .glass-input {
            background: rgba(15, 23, 42, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
        }
        .glass-input:focus { border-color: #3b82f6; box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1); }
        
        .custom-scrollbar::-webkit-scrollbar { width: 5px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #3b82f6; border-radius: 10px; }
    </style>
</head>
<body class="main-bg text-white">
    <div class="fixed-bg-layer"></div>

    <div id="loader" class="page-loader"></div>

    <div class="fixed top-0 left-0 right-0 z-[100] px-6 py-4">
        <div class="max-w-7xl mx-auto flex justify-between items-center backdrop-blur-md bg-[#0f172a]/80 border border-white/10 px-6 py-4 rounded-3xl shadow-2xl">
            <div class="flex items-center gap-3">
                <a href="/" class="group flex items-center gap-2 bg-slate-900/80 hover:bg-blue-600 px-5 py-2.5 rounded-2xl transition-all border border-slate-700">
                    <i class="fas fa-arrow-left text-blue-400 group-hover:text-white transition-transform group-hover:-translate-x-1"></i>
                    <span class="font-bold text-xs uppercase tracking-widest group-hover:text-white text-white">Asosiy</span>
                </a>
                <% if (locals.isLoggedIn) { %>
                    <div class="hidden sm:flex items-center gap-2 bg-emerald-500/10 border border-emerald-500/20 px-4 py-2.5 rounded-2xl">
                        <span class="text-emerald-400 font-black uppercase text-[10px] tracking-tighter">Admin Rejim</span>
                        <a href="/logout" class="text-red-400 hover:text-red-300 text-[10px] font-black uppercase border-l border-white/10 pl-3">Chiqish</a>
                    </div>
                <% } %>
            </div>
            
            <button onclick="toggleCart()" id="cart-btn" class="bg-blue-600 border border-blue-400 px-6 py-3 rounded-2xl flex items-center gap-3 hover:bg-blue-500 transition-all shadow-xl shadow-blue-600/30 group">
                <i class="fas fa-shopping-basket text-white group-hover:rotate-12 transition-transform"></i>
                <span id="cart-counter" class="text-sm font-black uppercase tracking-wider text-white">Savatcha: 0</span>
            </button>
        </div>
    </div>

    <div class="pt-28">
        <header class="text-center mt-10 mb-16" data-aos="zoom-in">
            <h1 class="text-6xl md:text-7xl font-black uppercase tracking-tighter">
                Market<span class="text-blue-500">place</span>
            </h1>
            <div class="w-20 h-1.5 bg-blue-600 mx-auto mt-4 rounded-full"></div>
            <p class="text-slate-400 text-sm mt-4 font-bold uppercase tracking-[0.3em] opacity-80">Innovatsion mahsulotlar platformasi</p>
        </header>

        <% if (locals.isLoggedIn) { %>
        <div class="max-w-4xl mx-auto px-6 mb-16" data-aos="fade-up">
            <div class="bg-slate-800/40 backdrop-blur-2xl p-10 rounded-[3rem] border border-blue-500/30 shadow-2xl">
                <h2 class="text-2xl font-bold mb-8 flex items-center gap-3 text-blue-400 uppercase tracking-tight">
                    <i class="fas fa-cart-plus"></i> Yangi mahsulot qo'shish
                </h2>
                <form action="/admin/add-item" method="POST" enctype="multipart/form-data" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                    <input type="hidden" name="type" value="product">
                    <div class="space-y-2">
                        <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Mahsulot nomi</label>
                        <input type="text" name="title" placeholder="Masalan: Smart Sensor" required class="glass-input w-full p-4 rounded-2xl outline-none text-white">
                    </div>
                    <div class="space-y-2">
                        <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Narxi (faqat raqam)</label>
                        <input type="text" name="price" placeholder="1200000" required class="glass-input w-full p-4 rounded-2xl outline-none text-white">
                    </div>
                    <div class="space-y-2">
                        <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Kategoriya</label>
                        <input type="text" name="status" placeholder="Elektronika" class="glass-input w-full p-4 rounded-2xl outline-none text-white">
                    </div>
                    <div class="space-y-2">
                        <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Rasm tanlang</label>
                        <div class="bg-slate-900/40 p-2.5 rounded-2xl border border-slate-700">
                            <input type="file" name="image" accept="image/*" class="w-full text-xs text-slate-400 file:mr-4 file:py-2 file:px-4 file:rounded-xl file:border-0 file:text-xs file:font-black file:bg-blue-600 file:text-white cursor-pointer">
                        </div>
                    </div>
                    <div class="md:col-span-2 space-y-2">
                        <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Batafsil tavsif</label>
                        <textarea name="description" placeholder="Mahsulot haqida ma'lumot..." required class="glass-input w-full p-4 rounded-2xl outline-none text-white h-28"></textarea>
                    </div>
                    <button type="submit" class="md:col-span-2 bg-gradient-to-r from-blue-700 to-blue-500 py-5 rounded-2xl font-black text-white shadow-xl shadow-blue-500/20 uppercase tracking-widest active:scale-95 transition-all">
                        Bazaga kiritish
                    </button>
                </form>
            </div>
        </div>
        <% } %>

        <main class="max-w-7xl mx-auto px-6 pb-32">
            <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10 items-start">
                <% if (locals.products && products.length > 0) { %>
                    <% products.forEach((product, index) => { %>
                        <div class="product-card p-8 group flex flex-col relative" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
                            <% if (locals.isLoggedIn) { %>
                                <form action="/admin/delete/product/<%= product.id %>" method="POST" onsubmit="return confirm('O\'chirilsinmi?')" class="absolute top-6 right-6 z-10">
                                    <button type="submit" class="bg-red-500/20 hover:bg-red-500 text-red-500 hover:text-white w-10 h-10 rounded-2xl transition-all flex items-center justify-center border border-red-500/30 backdrop-blur-md">
                                        <i class="fas fa-trash-alt"></i>
                                    </button>
                                </form>
                            <% } %>

                            <div class="relative overflow-hidden rounded-[2rem] mb-8 shadow-2xl aspect-video border border-white/5">
                                <img src="<%= product.image %>" class="w-full h-full object-cover group-hover:scale-110 transition duration-700" onerror="this.src='/images/bino.jpeg'">
                                <div class="absolute inset-0 bg-gradient-to-t from-[#0f172a]/80 to-transparent"></div>
                                <span class="absolute bottom-4 left-4 bg-blue-600/80 backdrop-blur-md text-[9px] font-black px-4 py-1.5 rounded-full uppercase tracking-widest text-white">
                                    <%= product.status || 'Mahsulot' %>
                                </span>
                            </div>
                            
                            <h3 class="text-2xl font-bold mb-3 group-hover:text-blue-400 transition-colors"><%= product.title %></h3>
                            
                            <p class="description-text text-slate-400 text-sm leading-relaxed mb-2" id="desc-<%= product.id %>">
                                <%= product.description %>
                            </p>
                            <span class="btn-more" onclick="toggleDescription('<%= product.id %>')">Batafsil</span>
                            
                            <div class="flex items-center justify-between pt-6 border-t border-white/10 mt-auto">
                                <div>
                                    <span class="text-[9px] text-slate-500 uppercase font-black block tracking-tighter mb-1">Narxi</span>
                                    <span class="text-2xl font-black text-white">
                                        <%= product.price || product.goal %> <span class="text-blue-500 text-sm italic ml-1">so'm</span>
                                    </span>
                                </div>
                                <button onclick="addToCart(this)" 
                                        data-title="<%= product.title %>"
                                        data-price="<%= product.price || product.goal %>"
                                        class="bg-blue-600 hover:bg-white hover:text-blue-600 text-white w-14 h-14 rounded-2xl transition-all flex items-center justify-center active:scale-90 shadow-lg shadow-blue-600/20">
                                    <i class="fas fa-cart-plus text-xl"></i>
                                </button>
                            </div>
                        </div>
                    <% }) %>
                <% } else { %>
                    <div class="col-span-full text-center py-32 bg-white/5 rounded-[4rem] border border-dashed border-white/10">
                        <i class="fas fa-shopping-bag text-5xl text-slate-700 mb-6"></i>
                        <p class="text-slate-500 font-bold uppercase tracking-widest italic">Hozircha sotuvda mahsulotlar yo'q.</p>
                    </div>
                <% } %>
            </div>
        </main>
    </div>

    <div id="cart-modal" onclick="if(event.target == this) toggleCart()">
        <div class="bg-slate-900 border border-white/10 w-full max-w-lg rounded-[3.5rem] p-10 mx-4 shadow-2xl relative">
            <div class="flex justify-between items-center mb-10">
                <h2 class="text-3xl font-black uppercase tracking-tight text-white">Savatcha<span class="text-blue-500">.</span></h2>
                <button onclick="toggleCart()" class="text-2xl text-slate-400 hover:text-white">&times;</button>
            </div>
            <div id="cart-items" class="space-y-4 max-h-[400px] overflow-y-auto pr-4 mb-10 custom-scrollbar text-white"></div>
            <div class="border-t border-white/10 pt-8">
                <div class="flex justify-between items-end mb-8">
                    <span class="text-slate-500 font-bold uppercase text-xs tracking-widest">Jami:</span>
                    <span id="total-price" class="text-3xl font-black text-blue-500">0 so'm</span>
                </div>
                <button onclick="checkout()" class="w-full bg-blue-600 hover:bg-blue-500 py-5 rounded-[2rem] font-black text-lg transition-all shadow-xl shadow-blue-600/30 uppercase tracking-widest text-white">
                    Tasdiqlash
                </button>
            </div>
        </div>
    </div>

    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ once: true, duration: 1000 });

        window.addEventListener('load', () => {
            document.getElementById('loader').classList.add('hidden');
        });

        function toggleDescription(id) {
            const desc = document.getElementById('desc-' + id);
            const btn = desc.nextElementSibling;
            desc.classList.toggle('expanded');
            btn.innerText = desc.classList.contains('expanded') ? "Yopish" : "Batafsil";
        }

        let cart = [];
        function addToCart(btn) {
            const title = btn.getAttribute('data-title');
            const price = btn.getAttribute('data-price');
            cart.push({ title, price });
            updateCartUI();
            
            const cartBtn = document.getElementById('cart-btn');
            cartBtn.classList.remove('animate-pop');
            void cartBtn.offsetWidth; 
            cartBtn.classList.add('animate-pop');
        }

        function updateCartUI() {
            document.getElementById('cart-counter').innerText = `Savatcha: ${cart.length}`;
            const list = document.getElementById('cart-items');
            
            if (cart.length === 0) {
                list.innerHTML = `<p class="text-center text-slate-500 italic">Savatcha bo'sh</p>`;
                document.getElementById('total-price').innerText = "0 so'm";
                return;
            }
            
            list.innerHTML = cart.map((item, index) => `
                <div class="flex justify-between items-center bg-white/5 p-4 rounded-2xl border border-white/5">
                    <div>
                        <div class="text-sm font-bold uppercase text-white">${item.title}</div>
                        <div class="text-blue-400 text-xs">${item.price} so'm</div>
                    </div>
                    <button onclick="removeItem(${index})" class="text-red-500 p-2"><i class="fas fa-trash-alt"></i></button>
                </div>
            `).join('');

            let total = cart.reduce((sum, item) => {
                let rawPrice = item.price ? item.price.toString() : "0";
                let cleanPrice = rawPrice.replace(/[^0-9]/g, '');
                return sum + (parseInt(cleanPrice) || 0);
            }, 0);
            document.getElementById('total-price').innerText = total.toLocaleString() + " so'm";
        }

        function removeItem(index) { cart.splice(index, 1); updateCartUI(); }
        function toggleCart() {
            const modal = document.getElementById('cart-modal');
            modal.style.display = (modal.style.display === 'flex') ? 'none' : 'flex';
        }

        function checkout() {
            if (cart.length === 0) return alert("Savatcha bo'sh!");
            let msg = "🆕 YANGI BUYURTMA\n\n";
            cart.forEach((it, i) => msg += `📦 ${it.title} - ${it.price} so'm\n`);
            msg += `\n💰 Jami: ${document.getElementById('total-price').innerText}`;
            window.open(`https://t.me/InnovatHubBot?text=${encodeURIComponent(msg)}`, '_blank');
        }
    </script>
</body>
=======
<!DOCTYPE html>
<html lang="uz">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Marketplace | TIQXMMI</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://unpkg.com/aos@2.3.1/dist/aos.css" rel="stylesheet">
    <style>
        body, html {
            height: 100%;
            margin: 0;
            font-family: 'Inter', sans-serif;
            background-color: #0f172a;
        }

        .page-loader {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: #0f172a; z-index: 9999;
            transition: transform 0.8s cubic-bezier(0.77, 0, 0.175, 1);
            transform-origin: bottom;
        }
        .page-loader.hidden { transform: scaleY(0); }
        .loader-active { transform: scaleY(1) !important; transform-origin: top !important; }

        .main-bg {
            background: linear-gradient(rgba(15, 23, 42, 0.85), rgba(15, 23, 42, 0.95)), 
                        url('/marketplace_background.JPG');
            background-size: cover; background-position: center; background-attachment: fixed;
            min-height: 100vh;
        }

        .product-card {
            background: rgba(255, 255, 255, 0.03);
            backdrop-filter: blur(15px);
            border: 1px solid rgba(255, 255, 255, 0.08);
            border-radius: 2.5rem;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }

        .product-card:hover {
            border-color: rgba(59, 130, 246, 0.5);
            background: rgba(255, 255, 255, 0.07);
            transform: translateY(-10px);
            box-shadow: 0 20px 40px -15px rgba(59, 130, 246, 0.3);
        }

        @keyframes cartPop {
            0% { transform: scale(1); }
            50% { transform: scale(1.2); }
            100% { transform: scale(1); }
        }
        .animate-pop { animation: cartPop 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275); }

        #cart-modal {
            display: none; position: fixed; inset: 0; z-index: 10000;
            background: rgba(15, 23, 42, 0.9); backdrop-filter: blur(20px);
            align-items: center; justify-content: center;
        }

        .glass-input {
            background: rgba(15, 23, 42, 0.6);
            border: 1px solid rgba(255, 255, 255, 0.1);
            transition: all 0.3s ease;
        }
        .glass-input:focus { border-color: #3b82f6; box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.1); }
        
        .custom-scrollbar::-webkit-scrollbar { width: 5px; }
        .custom-scrollbar::-webkit-scrollbar-track { background: transparent; }
        .custom-scrollbar::-webkit-scrollbar-thumb { background: #3b82f6; border-radius: 10px; }
    </style>
</head>
<body class="main-bg text-white">
    <div id="loader" class="page-loader"></div>

    <div class="max-w-7xl mx-auto px-6 py-6 flex justify-between items-center sticky top-0 z-50 backdrop-blur-md bg-[#0f172a]/40 rounded-b-3xl">
        <div class="flex items-center gap-3">
            <a href="/" class="group flex items-center gap-2 bg-slate-900/80 hover:bg-blue-600 px-5 py-2.5 rounded-2xl transition-all border border-slate-700">
                <i class="fas fa-arrow-left text-blue-400 group-hover:text-white transition-transform group-hover:-translate-x-1"></i>
                <span class="font-bold text-xs uppercase tracking-widest group-hover:text-white">Asosiy</span>
            </a>
            <% if (locals.isLoggedIn) { %>
                <div class="hidden sm:flex items-center gap-2 bg-emerald-500/10 border border-emerald-500/20 px-4 py-2.5 rounded-2xl">
                    <span class="text-emerald-400 font-black uppercase text-[10px] tracking-tighter">Admin Rejim</span>
                    <a href="/logout" class="text-red-400 hover:text-red-300 text-[10px] font-black uppercase border-l border-white/10 pl-3">Chiqish</a>
                </div>
            <% } %>
        </div>
        
        <button onclick="toggleCart()" id="cart-btn" class="bg-blue-600 border border-blue-400 px-6 py-3 rounded-2xl flex items-center gap-3 hover:bg-blue-500 transition-all shadow-xl shadow-blue-600/30 group">
            <i class="fas fa-shopping-basket group-hover:rotate-12 transition-transform"></i>
            <span id="cart-counter" class="text-sm font-black uppercase tracking-wider">Savatcha: 0</span>
        </button>
    </div>

    <header class="text-center mt-10 mb-16" data-aos="zoom-in">
        <h1 class="text-6xl md:text-7xl font-black uppercase tracking-tighter">
            Market<span class="text-blue-500">place</span>
        </h1>
        <div class="w-20 h-1.5 bg-blue-600 mx-auto mt-4 rounded-full"></div>
        <p class="text-slate-400 text-sm mt-4 font-bold uppercase tracking-[0.3em] opacity-80">Innovatsion mahsulotlar platformasi</p>
    </header>

    <% if (locals.isLoggedIn) { %>
    <div class="max-w-4xl mx-auto px-6 mb-16" data-aos="fade-up">
        <div class="bg-slate-800/40 backdrop-blur-2xl p-10 rounded-[3rem] border border-blue-500/30 shadow-2xl">
            <h2 class="text-2xl font-bold mb-8 flex items-center gap-3 text-blue-400 uppercase tracking-tight">
                <i class="fas fa-cart-plus"></i> Yangi mahsulot qo'shish
            </h2>
            <form action="/admin/add-item" method="POST" enctype="multipart/form-data" class="grid grid-cols-1 md:grid-cols-2 gap-6">
                <input type="hidden" name="type" value="product">
                <div class="space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Mahsulot nomi</label>
                    <input type="text" name="title" placeholder="Masalan: Smart Sensor" required class="glass-input w-full p-4 rounded-2xl outline-none text-white">
                </div>
                <div class="space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Narxi (faqat raqam)</label>
                    <input type="text" name="price" placeholder="1200000" required class="glass-input w-full p-4 rounded-2xl outline-none text-white">
                </div>
                <div class="space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Kategoriya</label>
                    <input type="text" name="status" placeholder="Elektronika" class="glass-input w-full p-4 rounded-2xl outline-none text-white">
                </div>
                <div class="space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Rasm tanlang</label>
                    <div class="bg-slate-900/40 p-2.5 rounded-2xl border border-slate-700">
                        <input type="file" name="image" accept="image/*" class="w-full text-xs text-slate-400 file:mr-4 file:py-2 file:px-4 file:rounded-xl file:border-0 file:text-xs file:font-black file:bg-blue-600 file:text-white cursor-pointer">
                    </div>
                </div>
                <div class="md:col-span-2 space-y-2">
                    <label class="text-[10px] font-bold uppercase text-slate-500 ml-2">Batafsil tavsif</label>
                    <textarea name="description" placeholder="Mahsulot haqida ma'lumot..." required class="glass-input w-full p-4 rounded-2xl outline-none text-white h-28"></textarea>
                </div>
                <button type="submit" class="md:col-span-2 bg-gradient-to-r from-blue-700 to-blue-500 py-5 rounded-2xl font-black text-white shadow-xl shadow-blue-500/20 uppercase tracking-widest active:scale-95 transition-all">
                    Bazaga kiritish
                </button>
            </form>
        </div>
    </div>
    <% } %>

    <main class="max-w-7xl mx-auto px-6 pb-32">
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-10">
            <% if (locals.products && products.length > 0) { %>
                <% products.forEach((product, index) => { %>
                    <div class="product-card p-8 group flex flex-col relative" data-aos="fade-up" data-aos-delay="<%= index * 100 %>">
                        <% if (locals.isLoggedIn) { %>
                            <form action="/admin/delete/product/<%= product.id %>" method="POST" onsubmit="return confirm('O\'chirilsinmi?')" class="absolute top-6 right-6 z-10">
                                <button type="submit" class="bg-red-500/20 hover:bg-red-500 text-red-500 hover:text-white w-10 h-10 rounded-2xl transition-all flex items-center justify-center border border-red-500/30 backdrop-blur-md">
                                    <i class="fas fa-trash-alt"></i>
                                </button>
                            </form>
                        <% } %>

                        <div class="relative overflow-hidden rounded-[2rem] mb-8 shadow-2xl aspect-video border border-white/5">
                            <img src="<%= product.image %>" class="w-full h-full object-cover group-hover:scale-110 transition duration-700" onerror="this.src='/images/bino.jpeg'">
                            <div class="absolute inset-0 bg-gradient-to-t from-[#0f172a]/80 to-transparent"></div>
                            <span class="absolute bottom-4 left-4 bg-blue-600/80 backdrop-blur-md text-[9px] font-black px-4 py-1.5 rounded-full uppercase tracking-widest">
                                <%= product.status || 'Mahsulot' %>
                            </span>
                        </div>
                        
                        <h3 class="text-2xl font-bold mb-3 group-hover:text-blue-400 transition-colors"><%= product.title %></h3>
                        <p class="text-slate-400 text-sm leading-relaxed mb-8 flex-grow line-clamp-3"><%= product.description %></p>
                        
                        <div class="flex items-center justify-between pt-6 border-t border-white/10">
                            <div>
                                <span class="text-[9px] text-slate-500 uppercase font-black block tracking-tighter mb-1">Narxi</span>
                                <span class="text-2xl font-black text-white">
                                    <%= product.price || product.goal %> <span class="text-blue-500 text-sm italic ml-1">so'm</span>
                                </span>
                            </div>
                            <button onclick="addToCart(this)" 
                                    data-title="<%= product.title %>"
                                    data-price="<%= product.price || product.goal %>"
                                    class="bg-blue-600 hover:bg-white hover:text-blue-600 text-white w-14 h-14 rounded-2xl transition-all flex items-center justify-center active:scale-90 shadow-lg shadow-blue-600/20">
                                <i class="fas fa-cart-plus text-xl"></i>
                            </button>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="col-span-full text-center py-32 bg-white/5 rounded-[4rem] border border-dashed border-white/10">
                    <i class="fas fa-shopping-bag text-5xl text-slate-700 mb-6"></i>
                    <p class="text-slate-500 font-bold uppercase tracking-widest italic">Hozircha sotuvda mahsulotlar yo'q.</p>
                </div>
            <% } %>
        </div>
    </main>

    <div id="cart-modal" onclick="if(event.target == this) toggleCart()">
        <div class="bg-slate-900 border border-white/10 w-full max-w-lg rounded-[3.5rem] p-10 mx-4 shadow-2xl relative">
            <div class="flex justify-between items-center mb-10">
                <h2 class="text-3xl font-black uppercase tracking-tight">Savatcha<span class="text-blue-500">.</span></h2>
                <button onclick="toggleCart()" class="text-2xl text-slate-400 hover:text-white">&times;</button>
            </div>
            <div id="cart-items" class="space-y-4 max-h-[400px] overflow-y-auto pr-4 mb-10 custom-scrollbar text-white"></div>
            <div class="border-t border-white/10 pt-8">
                <div class="flex justify-between items-end mb-8">
                    <span class="text-slate-500 font-bold uppercase text-xs tracking-widest">Jami:</span>
                    <span id="total-price" class="text-3xl font-black text-blue-500">0 so'm</span>
                </div>
                <button onclick="checkout()" class="w-full bg-blue-600 hover:bg-blue-500 py-5 rounded-[2rem] font-black text-lg transition-all shadow-xl shadow-blue-600/30 uppercase tracking-widest">
                    Tasdiqlash
                </button>
            </div>
        </div>
    </div>

    <script src="https://unpkg.com/aos@2.3.1/dist/aos.js"></script>
    <script>
        AOS.init({ once: true, duration: 1000 });

        window.addEventListener('load', () => {
            document.getElementById('loader').classList.add('hidden');
        });

        let cart = [];
        function addToCart(btn) {
            const title = btn.getAttribute('data-title');
            const price = btn.getAttribute('data-price');
            cart.push({ title, price });
            updateCartUI();
            
            const cartBtn = document.getElementById('cart-btn');
            cartBtn.classList.remove('animate-pop');
            void cartBtn.offsetWidth; 
            cartBtn.classList.add('animate-pop');
        }

        function updateCartUI() {
            document.getElementById('cart-counter').innerText = `Savatcha: ${cart.length}`;
            const list = document.getElementById('cart-items');
            
            if (cart.length === 0) {
                list.innerHTML = `<p class="text-center text-slate-500 italic">Savatcha bo'sh</p>`;
                document.getElementById('total-price').innerText = "0 so'm";
                return;
            }
            
            list.innerHTML = cart.map((item, index) => `
                <div class="flex justify-between items-center bg-white/5 p-4 rounded-2xl border border-white/5">
                    <div>
                        <div class="text-sm font-bold uppercase">${item.title}</div>
                        <div class="text-blue-400 text-xs">${item.price} so'm</div>
                    </div>
                    <button onclick="removeItem(${index})" class="text-red-500 p-2"><i class="fas fa-trash-alt"></i></button>
                </div>
            `).join('');

            let total = cart.reduce((sum, item) => {
                // Xavfsiz narx hisoblash
                let rawPrice = item.price ? item.price.toString() : "0";
                let cleanPrice = rawPrice.replace(/[^0-9]/g, '');
                return sum + (parseInt(cleanPrice) || 0);
            }, 0);
            document.getElementById('total-price').innerText = total.toLocaleString() + " so'm";
        }

        function removeItem(index) { cart.splice(index, 1); updateCartUI(); }
        function toggleCart() {
            const modal = document.getElementById('cart-modal');
            modal.style.display = (modal.style.display === 'flex') ? 'none' : 'flex';
        }

        function checkout() {
            if (cart.length === 0) return alert("Savatcha bo'sh!");
            let msg = "🆕 YANGI BUYURTMA\n\n";
            cart.forEach((it, i) => msg += `📦 ${it.title} - ${it.price} so'm\n`);
            msg += `\n💰 Jami: ${document.getElementById('total-price').innerText}`;
            window.open(`https://t.me/InnovatHubBot?text=${encodeURIComponent(msg)}`, '_blank');
        }
    </script>
</body>
>>>>>>> bec3915a8d75b2b7855842e7b1cd3461e2bc096d
</html>